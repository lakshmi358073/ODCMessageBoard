<h1>Listing create_categories</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @create_categories.each do |create_category| %>
  <tr>
    <td><%= create_category.name %></td>
    <td><%= link_to 'Show', create_category %></td>
    <td><%= link_to 'Edit', edit_create_category_path(create_category) %></td>
    <td><%= link_to 'Destroy', create_category, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>

</table>



<br/>
<br/>
<%= link_to 'Back', post1s_path %>
